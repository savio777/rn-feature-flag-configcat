import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
    padding: 8,
  },
  title: {
    fontWeight: 'bold',
    fontSize: 22,
  },
  banner: {
    width: '100%',
    height: '30%',
    elevation: 1,
  },
});
